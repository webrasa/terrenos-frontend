export type IndexTranslations = {
  general: { title: string; description: string };
  heroSection: { title: string; inputPlaceholder: string };
  sliderSection: { title: string };
  tripSection: { title: string; description: string; ctaTitle: string };
  searchSection: { title: string };
};

export type NotFoundTranslations = {
  notFound: string;
};

export type ProfileTranslations = {
  general: { title: string; description: string };
  menuSection: {
    currentListingsTitle: string;
    draftsTitle: string;
    myWatchListTitle: string;
    offTheMarketTitle: string;
    inboxTitle: string;
    accountTitle: string;
  };
  inboxSection: {
    myListingsSubtitle: string;
    sentByYouSubtitle: string;
    sendMessagePlaceholder: string;
    sendButtonTitle: string;
  };
};
