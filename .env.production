# FIXME: Configure environment variable for production

# For security reason, don't push secret key in your git repo.
# Instead create a file named `.env.development.local` and add your secret key.

# Amplify configuration
NEXT_PUBLIC_AWS_REGION=us-east-1
NEXT_PUBLIC_AWS_API_GATEWAY_URL=

# Stripe configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUBSCRIPTION_PRICE_ENV=prod # Load stripe subscription configuration based on environement
